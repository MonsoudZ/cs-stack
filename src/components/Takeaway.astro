<p class="takeaway"><span>Takeaway</span><slot /></p>

<style>
  .takeaway{font-family:var(--mono);font-size:13px;line-height:1.65;color:var(--dim);margin:16px 0 0;
    border:1px solid var(--border);border-radius:999px;padding:9px 14px;background:var(--fill);max-width:max-content}
  .takeaway span{font-size:11px;letter-spacing:.14em;text-transform:uppercase;color:var(--accent,var(--signal));margin-right:9px}
  @media(max-width:680px){.takeaway{border-radius:12px;max-width:none}}
</style>
